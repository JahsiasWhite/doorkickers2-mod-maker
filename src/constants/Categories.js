/**
 * This is currently used to discern between weapon classes in code.
 *  Needed for doctrine abilities and some actions, like we can only breach a door with shotguns.
 */
const Categories = ['pistol', 'rifle', 'shotgun', 'rpg'];

export default Categories;
